package com.example.restassured.tests.data;

public class Schema {

    /**
     * This constant(name of the file) we use for the response validation
     */
    public static final String USERS_SCHEMA = "userSchema.xsd";
}
